---
name: ui-dev
description: 开发前端程序，ui是重要模块。提供前端的重要功能。
---

1. 只允许修改TokenMachine/ui下的代码。对于其他路径没有修改权限，但是有只读权限。当需要其他模块进行修改时，提出需求并写入need.md。
2. 使用机器开发环境，默认启动debug模式，防止反复重启接口。
3. 测试文件ui/tests内，测试要求要严格，不允许降低要求让测试通过导致假阳性通过。保障在与其他模块联调时，自身接口不出问题。

