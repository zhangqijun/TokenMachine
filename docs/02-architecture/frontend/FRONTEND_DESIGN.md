# TokenMachine 前端设计文档

> 企业级 AI 模型部署管理平台 Web UI 设计方案

---

## 目录

- [1. 设计概述](#1-设计概述)
- [2. 页面架构设计](#2-页面架构设计)
- [3. 页面详细设计](#3-页面详细设计)
- [4. 交互设计](#4-交互设计)
- [5. 数据流与状态管理](#5-数据流与状态管理)
- [6. 组件设计](#6-组件设计)
- [7. 视觉设计规范](#7-视觉设计规范)
- [8. 响应式设计](#8-响应式设计)
- [9. 性能优化策略](#9-性能优化策略)

---

## 1. 设计概述

### 1.1 设计理念

TokenMachine 前端设计遵循以下核心原则：

| 原则 | 说明 | 应用场景 |
|------|------|----------|
| **专业性** | 企业级界面标准，严谨的视觉层次 | 数据展示、表单设计 |
| **效率优先** | 减少操作步骤，快速完成任务 | 模型部署、配置调整 |
| **实时反馈** | 数据实时更新，状态即时可见 | GPU 监控、部署状态 |
| **容错性** | 明确的错误提示，预防误操作 | 删除确认、表单验证 |
| **一致性** | 统一的交互模式和视觉语言 | 全局导航、操作按钮 |

### 1.2 用户角色

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| **系统管理员** | 负责平台运维和资源管理 | 快速定位问题、资源状态一目了然 |
| **算法工程师** | 负责模型部署和测试 | 简化部署流程、便捷的调试工具 |
| **开发者** | 使用 API 进行集成 | 清晰的 API 文档、便捷的 Key 管理 |

### 1.3 信息架构

```
TokenMachine Web UI
│
├── 公开页面
│   ├── 登录页
│   └── API 文档 (Swagger)
│
└── 应用主界面 (Authenticated)
    │
    ├── 顶部导航栏
    │   ├── Logo
    │   ├── 主导航菜单
    │   ├── 用户信息
    │   └── 通知中心
    │
    └── 内容区域
        │
        ├── 仪表盘 (Dashboard)
        │   ├── 系统概览卡片
        │   ├── GPU 监控图表
        │   ├── 模型运行状态
        │   └── API 调用量趋势
        │
        ├── 模型管理 (Models)
        │   ├── 模型列表
        │   ├── 模型上传/下载
        │   ├── 部署配置
        │   └── 部署详情
        │
        ├── 集群管理 (Cluster)
        │   ├── Worker 节点列表
        │   ├── 节点添加/下线
        │   ├── GPU 资源视图
        │   └── 节点详情
        │
        ├── API 密钥 (API Keys)
        │   ├── 密钥列表
        │   ├── 创建密钥
        │   ├── 使用统计
        │   └── 权限配置
        │
        ├── 测试场 (Playground)
        │   ├── 模型选择
        │   ├── 参数配置
        │   └── 对话测试
        │
        └── 系统设置 (Settings)
            ├── 用户管理
            ├── 系统配置
            └── 操作日志
```

---

## 2. 页面架构设计

### 2.1 整体布局

采用经典的 **侧边栏 + 顶部栏 + 内容区** 布局：

```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏 (56px)                                        │
│  Logo | 主导航 |             用户信息 通知          │
├──────┬──────────────────────────────────────────────────┤
│      │                                                  │
│ 侧  │                                                  │
│ 边  │          内容区域                                 │
│ 栏  │          (自适应高度)                             │
│      │                                                  │
│(240)│                                                  │
│      │                                                  │
│      │                                                  │
└──────┴──────────────────────────────────────────────────┘
```

**布局特点**：
- **固定高度导航栏**：始终可见，提供全局导航
- **可折叠侧边栏**：支持收起/展开，最大化内容区域
- **全屏内容区**：使用剩余所有空间，支持内部滚动
- **面包屑导航**：二级页面显示路径，方便返回

### 2.2 导航结构

#### 2.2.1 顶部导航栏

**位置**：固定在顶部，高度 56px

**组成元素**：
| 区域 | 内容 | 交互 |
|------|------|------|
| **左侧** | Logo、产品名称 | 点击回到首页 |
| **中间** | 主导航菜单 | 高亮当前页面 |
| **右侧** | 搜索框、帮助文档、通知图标、用户头像 | 下拉菜单/跳转 |

#### 2.2.2 侧边栏

**位置**：左侧固定，宽度 240px（可折叠至 64px）

**菜单结构**：
```
📊 仪表盘
🤖 模型管理
🖥️  集群管理
🔑 API 密钥
🧪 测试场
⚙️  系统设置
```

**折叠状态**：
- 展开时：显示图标 + 文字
- 折叠时：仅显示图标，悬停显示文字提示

### 2.3 路由设计

| 路径 | 页面 | 权限 |
|------|------|------|
| `/login` | 登录页 | 公开 |
| `/` | 仪表盘 | 需认证 |
| `/models` | 模型列表 | 需认证 |
| `/models/:id` | 模型详情 | 需认证 |
| `/models/new` | 上传模型 | 需认证 |
| `/deployments` | 部署列表 | 需认证 |
| `/deployments/:id` | 部署详情 | 需认证 |
| `/deployments/new` | 创建部署 | 需认证 |
| `/cluster` | 集群概览 | 需认证 |
| `/cluster/worker/:id` | Worker 详情 | 需认证 |
| `/api-keys` | API 密钥列表 | 需认证 |
| `/playground` | 测试场 | 需认证 |
| `/settings` | 系统设置 | 需认证（管理员）|

---

## 3. 页面详细设计

### 3.1 登录页

**布局**：居中卡片式布局

```
┌─────────────────────────────────────────────┐
│                                             │
│           [Logo]                            │
│        TokenMachine                         │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │  用户名                               │  │
│  │  [____________________________]      │  │
│  │                                       │  │
│  │  密码                               │  │
│  │  [____________________________]      │  │
│  │                                       │  │
│  │  [      登 录      ]                 │  │
│  └───────────────────────────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

**交互细节**：
- 输入框聚焦时高亮边框
- 显示/隐藏密码按钮
- 记住密码选项（默认不勾选）
- 登录失败显示错误提示
- 登录按钮 Loading 状态

---

### 3.2 仪表盘 (Dashboard)

**目的**：提供系统整体运行状态的一目了然视图

**布局**：4 列网格布局，卡片自适应排列

```
┌──────────────────────────────────────────────────────────┐
│  系统概览                                                 │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ 运行模型  │  │ 总 GPU   │  │ GPU 使用率│  │ 今日调用量│ │
│  │    12    │  │    48    │  │   76%    │  │  125.3K  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                          │
│  GPU 使用率趋势 (24小时)                                  │
│  ┌────────────────────────────────────────────────────┐ │
│  │  [折线图 - 多 GPU 使用率实时监控]                   │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌───────────────────────┐  ┌───────────────────────┐  │
│  │  模型运行状态          │  │  最近活动              │  │
│  │  ┌─────────────────┐  │  │  • 部署 llama-3-8b    │  │
│  │  │ llama-3-8b     │  │  │  • 停止 qwen-14b      │  │
│  │  │ [Running] ●    │  │  │  • 创建 API Key      │  │
│  │  │ GPU: 0,1       │  │  │  • Worker node-3 加入 │  │
│  │  └─────────────────┘  │  │                       │  │
│  │  ┌─────────────────┐  │  │                       │  │
│  │  │ qwen-14b       │  │  │                       │  │
│  │  │ [Running] ●    │  │  │                       │  │
│  │  │ GPU: 2,3       │  │  │                       │  │
│  │  └─────────────────┘  │  │                       │  │
│  └───────────────────────┘  └───────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

**卡片设计**：

| 卡片 | 指标 | 更新频率 | 交互 |
|------|------|----------|------|
| **运行模型** | 当前运行中的模型数量 | 实时 | 点击跳转部署列表 |
| **总 GPU** | 集群 GPU 总数 | 静态 | 点击跳转集群管理 |
| **GPU 使用率** | 平均 GPU 利用率 | 5秒 | 点击查看详情 |
| **今日调用量** | 今日 API 调用次数 | 1分钟 | 点击查看统计 |

**图表设计**：
- **GPU 使用率趋势**：多线折线图，每条线代表一个 GPU，X 轴为时间（24小时），Y 轴为使用率百分比
- **API 调用量趋势**：柱状图，按小时统计调用量

**数据更新**：
- 指标卡片：实时轮询（5秒间隔）
- 图表数据：每分钟更新一次
- 使用乐观更新 + 轮询补偿策略

---

### 3.3 模型管理

#### 3.3.1 模型列表页

**布局**：卡片网格 + 工具栏（参考 ModelScope 设计理念）

**卡片视图**：

```
┌─────────────────────────────────────────────────────────────────────┐
│  模型中心                                    [🔍 搜索模型...] [🔄]  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 分类筛选:                                                    │   │
│  │ [全部] [LLM] [Embedding] [Vision] [Audio] [多模态] [Rerank] │   │
│  │                                                              │   │
│  │ 任务类型:  [文本生成] [对话] [分类] [摘要] [翻译] [代码生成]  │   │
│  │                                                              │   │
│  │ 更多筛选: [状态 ▼] [引擎 ▼] [集群 ▼] [GPU 类型 ▼]          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────┐  ┌─────────────────────┐                │
│  │ [卡片视图] [列表视图]│  │ [▼ 部署新模型]      │  排序: [热度▼] │
│  └─────────────────────┘  └─────────────────────┘                │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  模型卡片网格布局 (3列)                                      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │                                                              │   │
│  │  ┌─────────────────────┐  ┌─────────────────────┐          │   │
│  │  │ 🏷️ [LLM]     运行中 │  │ 🏷️ [LLM]     运行中 │          │   │
│  │  │                     │  │                     │          │   │
│  │  │  llama-3-8b-instruct│  │  qwen-14b-chat      │          │   │
│  │  │                     │  │                     │          │   │
│  │  │  Meta AI            │  │  阿里云             │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ ⚡ 8B 参数           │  │ ⚡ 14B 参数          │          │   │
│  │  │ 📦 2/2 副本运行     │  │ 📦 1/1 副本运行     │          │   │
│  │  │ 💾 16GB 显存        │  │ 💾 28GB 显存        │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ 🔥 热度: 12.5K      │  │ 🔥 热度: 8.3K       │          │   │
│  │  │ 💬 调用: 45.2K      │  │ 💬 调用: 23.1K      │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ [💬 聊天] [▶️ API]  │  │ [💬 聊天] [▶️ API]  │          │   │
│  │  │ [⚙️ 配置] [⏸️ 停止] │  │ [⚙️ 配置] [⏸️ 停止] │          │   │
│  │  └─────────────────────┘  └─────────────────────┘          │   │
│  │                                                              │   │
│  │  ┌─────────────────────┐  ┌─────────────────────┐          │   │
│  │  │ 🏷️ [Embedding] 停止│  │ 🏷️ [Vision]   运行中│          │   │
│  │  │                     │  │                     │          │   │
│  │  │  bge-large-zh-v1.5  │  │  clip-vit-large     │          │   │
│  │  │                     │  │                     │          │   │
│  │  │  BAAI               │  │  OpenAI             │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ ⚡ 326M 参数         │  │ ⚡ 427M 参数         │          │   │
│  │  │ 📦 0/2 副本运行     │  │ 📦 1/1 副本运行     │          │   │
│  │  │ 💾 2GB 显存         │  │ 💾 1.5GB 显存       │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ 🔥 热度: 5.7K       │  │ 🔥 热度: 3.2K       │          │   │
│  │  │ 💬 调用: 18.9K      │  │ 💬 调用: 9.4K       │          │   │
│  │  │                     │  │                     │          │   │
│  │  │ [▶️ 启动] [⚙️ 配置]  │  │ [💬 聊天] [▶️ API]  │          │   │
│  │  │ [📥 下载] [🗑️ 删除] │  │ [⚙️ 配置] [⏸️ 停止] │          │   │
│  │  └─────────────────────┘  └─────────────────────┘          │   │
│  │                                                              │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  分页: [◀] 第 1/5 页 [▶]  每页 [12▼]  共 56 个模型                  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```



**列表视图（表格模式）**：

```
┌─────────────────────────────────────────────────────────────────────┐
│  模型列表                                    [🔍 搜索...] [🔄]      │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ [✓] 已选 3 个模型     [▼ 批量操作: 开始 | 停止 | 删除]      │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ [ ]│模型名称      │类型│副本│状态│显存│热度│调用│操作      │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ [✓]│llama-3-8b   │LLM │[2]│●  │16GB│12K │45K │[▼] 更多  │   │
│  │    │Meta AI      │    │    │运行│    │    │    │         │   │
│  │    │▼ 展开 2实例 │    │    │    │    │    │    │         │   │
│  │    │ • inst-001  │    │    │    │    │    │    │[日志]   │   │
│  │    │   worker-01 │    │    │    │    │    │    │         │   │
│  │    │ • inst-002  │    │    │    │    │    │    │[日志]   │   │
│  │    │   worker-02 │    │    │    │    │    │    │         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ [✓]│qwen-14b     │LLM │[1]│●  │28GB│8.3K│23K │[▼] 更多  │   │
│  │    │阿里云       │    │    │运行│    │    │    │         │   │
│  ├─────────────────────────────────────────────────────────────┤   │
│  │ [ ] │bge-large    │Emb │[0]│○  │ -  │5.7K│18K │[▼] 更多  │   │
│  │    │BAAI         │    │    │停止│    │    │    │         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  分页: [◀] 1/5 页 [▶]  每页 [10▼]  共 56 个模型                     │
└─────────────────────────────────────────────────────────────────────┘
```

**工具栏功能**：

| 位置 | 组件 | 功能 | 说明 |
|------|------|------|------|
| **左侧** | 搜索框 | 模型名称搜索 | 实时过滤，防抖 350ms |
| | 类别筛选 | 按类别过滤 | LLM、Embedding、Vision、Audio 等 |
| | 集群筛选 | 按集群过滤 | 多选下拉，显示已选数量 |
| | 状态筛选 | 按运行状态过滤 | 全部、运行中、已停止、异常 |
| | 刷新按钮 | 手动刷新数据 | 重新获取模型列表 |
| **右侧** | 部署模型 | 下拉菜单触发 | HuggingFace、ModelScope、本地文件 |
| | 批量操作 | 多选后启用 | 开始、停止、删除（显示选中数量） |

**表格功能**：

| 功能 | 说明 | 交互细节 |
|------|------|----------|
| **行选择** | 支持单选/全选 | 复选框，选中后启用批量操作 |
| **展开行** | 显示实例详情 | 点击展开图标，展示模型的所有实例 |
| **内联编辑** | 副本数编辑 | 直接点击副本数输入框，支持 + / - 调整 |
| **排序** | 点击表头排序 | 支持多列排序，按住 Shift 点击多列 |
| **分页** | 支持页码跳转 | 可调整每页条数（10/20/50/100） |
| **下拉菜单** | 每行的更多操作 | 编辑、聊天、API 访问、访问控制、开始、停止、删除 |

**实例详情（展开行）**：

```
┌──────────────────────────────────────────────────────────┐
│  实例列表：llama-3-8b                                     │
├──────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐  │
│  │ 实例 ID  │ Worker  │ GPU分配 │ 显存   │ 状态 │操作 │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ inst-001 │ worker-1│ gpu:0   │ 12.5GB │运行 │[▼] │  │
│  │          │         │ gpu:1   │ 12.5GB │      │    │  │
│  ├────────────────────────────────────────────────────┤  │
│  │ inst-002 │ worker-2│ gpu:0   │ 12.5GB │运行 │[▼] │  │
│  │          │         │         │        │      │    │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  实例操作菜单：                                           │
│  - 查看日志（实时日志流）                                 │
│  - 删除实例（销毁并重新创建）                             │
└──────────────────────────────────────────────────────────┘
```

**状态标识**：

| 状态 | 标签样式 | 说明 | 可执行操作 |
|------|----------|------|------------|
| **运行中** | 绿色圆点 ● | 至少有 1 个副本在运行 | 停止、编辑、聊天 |
| **已停止** | 灰色圆点 ○ | 副本数为 0 | 开始、编辑、删除 |
| **部分运行** | 橙色圆点 🟡 | Ready 副本 < 总副本数 | 停止、查看日志 |
| **异常** | 红色圆点 🔴 | 实例启动失败 | 查看日志、删除 |

**实时更新机制**：

| 更新类型 | 频率 | 方案 | 说明 |
|----------|------|------|------|
| **模型列表** | 页面可见时轮询 | Server-Sent Events (SSE) | 1 秒延迟后启动 chunk request |
| **实例状态** | 实时推送 | SSE chunk update | 增量更新，避免全量刷新 |
| **页面隐藏** | 暂停更新 | visibilitychange 监听 | 节省资源，切回时恢复 |
| **防抖搜索** | 350ms | debounce | 减少请求频率 |

**部署模型下拉菜单**：

```
┌────────────────────────┐
│ 从 HuggingFace 部署    │
│ 从 ModelScope 部署     │
│ 上传本地模型文件       │
├────────────────────────┤
│ 查看模型目录           │
└────────────────────────┘
```

**批量操作菜单**：

```
┌────────────────────────┐
│ 开始 (3)               │
│ 停止 (3)               │
├────────────────────────┤
│ 删除 (3)               │
└────────────────────────┘
```

**单行操作菜单**：

```
┌────────────────────────┐
│ ▶️ 开始                │ - 启动模型（副本数设为 1）
│ ⏸️ 停止                │ - 停止模型（副本数设为 0）
│ ├──────────────────────┤
│ ✏️ 编辑                │ - 修改模型配置
│ 💬 聊天                │ - 跳转到 Playground
│ 🔑 API 访问            │ - 查看 API 调用信息
│ 🔒 访问控制            │ - 配置访问权限
│ ├──────────────────────┤
│ 🗑️ 删除                │ - 删除模型
└────────────────────────┘
```

**空状态展示**：

| 场景 | 显示内容 | 操作引导 |
|------|----------|----------|
| **无集群** | 图标 + "暂无集群" | 提示先创建集群 |
| **无 Worker** | 图标 + "暂无 Worker 节点" | 引导添加 Worker |
| **无模型** | 图标 + "暂无模型" | "部署模型" 按钮 |
| **搜索无结果** | "未找到匹配的模型" | 清空搜索条件 |

**技术实现要点**：

```typescript
// 关键技术栈（卡片视图）
- 卡片网格：Ant Design Row + Col (响应式栅格系统)
- 卡片组件：Ant Design Card + 自定义 ModelCard
- 标签筛选：Ant Design Tag + Space (可点击标签)
- 视图切换：useState 管理视图模式（card/list）
- 响应式布局：Grid 系统（xs: 24, sm: 12, md: 8, lg: 6）

// 关键技术栈（列表视图）
- 表格组件：Ant Design Table + 自定义 SealTable
- 行选择：useTableRowSelection
- 展开行：useExpandedRowKeys
- 表格排序：useTableMultiSort (支持多列)

// 通用技术
- 状态管理：Zustand store (viewMode, filters, sortBy)
- 实时更新：useChunkRequest + useUpdateChunkedList
- 防抖搜索：lodash.debounce (350ms)
- 页面可见性：document.visibilityState
- 虚拟滚动：react-window (大数据量优化)
```

**性能优化**：

| 优化点 | 方案 | 效果 |
|--------|------|------|
| **分页加载** | 卡片视图每页 12 条，列表视图每页 10-20 条 | 减少首屏加载时间 |
| **增量更新** | SSE chunk request | 避免全量刷新 |
| **防抖搜索** | 350ms debounce | 减少无效请求 |
| **暂停轮询** | 页面隐藏时停止 | 节省 CPU/网络资源 |
| **虚拟滚动** | 大数据量时启用（列表视图） | 支持 1000+ 模型 |
| **懒加载图片** | 卡片缩略图使用 Intersection Observer | 减少初始加载带宽 |
| **React.memo** | 卡片组件使用 memo 包裹 | 避免不必要的重渲染 |
| **缓存策略** | sessionStorage 缓存 | 刷新时保留视图模式和筛选条件 |
| **响应式网格** | CSS Grid 响应式布局 | 自适应不同屏幕尺寸 |

#### 3.3.2 模型上传页

**布局**：分步表单，3 步完成上传

```
┌──────────────────────────────────────────────────────────┐
│  上传模型                                    步骤 1/3      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ● 模型信息  ○ 下载配置  ○ 确认上传                      │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  来源选择：                                        │ │
│  │  ⦿ 从 HuggingFace 下载                            │ │
│  │  ○ 从 ModelScope 下载                             │ │
│  │  ○ 上传本地文件                                    │ │
│  │                                                     │ │
│  │  模型名称：                                        │ │
│  │  [meta-llama/Llama-3-8B-Instruct          ]        │ │
│  │                                                     │ │
│  │  版本：                                            │ │
│  │  [v1.0                                        ]    │ │
│  │                                                     │ │
│  │  说明：                                            │ │
│  │  支持的模型格式：                                   │ │
│  │  • LLaMA 2/3                                       │ │
│  │  • Qwen 1.5/2                                      │ │
│  │  • Mistral / Mixtral                               │ │
│  │  • Baichuan 2                                      │ │
│  │  • ChatGLM 3/4                                     │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│                              [上一步]    [下一步  ]      │
└──────────────────────────────────────────────────────────┘
```

**步骤 1: 模型信息**
- 选择来源（HuggingFace / ModelScope / 本地上传）
- 输入模型名称（支持搜索提示）
- 输入版本号
- 模型说明和格式要求

**步骤 2: 下载配置**
- HuggingFace Token（可选，用于私有模型）
- 断点续传开关（默认开启）
- 下载后验证 MD5/SHA256（可选）

**步骤 3: 确认上传**
- 显示模型信息摘要
- 确认开始下载
- 显示下载进度

#### 3.3.3 部署配置页

**布局**：左侧配置面板，右侧实时预览

```
┌──────────────────────────────────────────────────────────┐
│  创建部署                                    步骤 2/2      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ● 选择模型  ● 配置参数                                   │
│                                                          │
│  ┌────────────────────┐  ┌────────────────────────────┐ │
│  │  基本信息          │  │  预估资源                  │ │
│  │                    │  │                            │ │
│  │  部署名称：        │  │  显存需求： ~16GB          │ │
│  │  [llama-3-prod  ]  │  │  推荐副本数： 2            │ │
│  │                    │  │  预估 QPS： 500+           │ │
│  │  推理引擎：        │  │                            │ │
│  │  ⦿ vLLM            │  │  [GPU 资源分配图]         │ │
│  │  ○ SGLang (P2)     │  │  GPU 0: ████████░░ 80%    │ │
│  │                    │  │  GPU 1: ████████░░ 80%    │ │
│  └────────────────────┘  └────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  副本配置                                          │ │
│  │  副本数量：[2] 副本 (每个副本占用 1 个 GPU)        │ │
│  │  GPU 分配：                                        │ │
│  │  ☑ gpu:0  ☑ gpu:1  ☐ gpu:2  ☐ gpu:3            │ │
│  │                                                     │ │
│  │  高级设置：                                        │ │
│  │  + 展开配置                                        │ │
│  │  • Tensor Parallel Size: [1]                      │ │
│  │  • Max Model Length: [4096] tokens                │ │
│  │  • GPU Memory Utilization: [0.90]                 │ │
│  │  • Trust Remote Code: ☐                          │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│                              [取消]      [创建部署]      │
└──────────────────────────────────────────────────────────┘
```

**配置项说明**：

| 分类 | 配置项 | 默认值 | 说明 |
|------|--------|--------|------|
| **基本信息** | 部署名称 | `{model}-prod` | 唯一标识 |
| | 推理引擎 | vLLM | 后端选择 |
| **副本配置** | 副本数量 | 2 | 并行实例数 |
| | GPU 选择 | 自动 | 手动指定 GPU |
| **性能调优** | Max Model Length | 4096 | 最大上下文长度 |
| | GPU Memory Util | 0.90 | 显存利用率 |
| | Tensor Parallel | 1 | 张量并行度 |

**智能推荐**：
- 根据模型大小自动推荐副本数
- 显示预估的显存占用和 QPS
- GPU 资源不足时给出警告

---

### 3.4 集群管理

#### 3.4.1 集群概览

**布局**：顶部统计 + Worker 列表

```
┌──────────────────────────────────────────────────────────┐
│  集群管理                                  [添加 Worker]  │
├──────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐ │
│  │ Workers  │  │ 总 GPU   │  │ 运行中   │  │ 异常      │ │
│  │    8     │  │   32     │  │    24    │  │    0     │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │ Worker 节点列表                                    │ │
│  ├────────────────────────────────────────────────────┤ │
│  │ 节点名称    │ 状态   │ GPU   │ 利用率 │ 显存   │操作│ │
│  ├────────────────────────────────────────────────────┤ │
│  │ worker-01   │ Ready  │ 4     │ 85%    │ 128GB  │[详情]│ │
│  │             │        │       │        │        │[下线]│ │
│  ├────────────────────────────────────────────────────┤ │
│  │ worker-02   │ Ready  │ 4     │ 72%    │ 128GB  │[详情]│ │
│  │             │        │       │        │        │[下线]│ │
│  ├────────────────────────────────────────────────────┤ │
│  │ worker-03   │ Busy   │ 4     │ 95%    │ 120GB  │[详情]│ │
│  │             │        │       │        │        │[排空]│ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

**状态标识**：
- **Ready**: 绿色，可用，可调度
- **Busy**: 蓝色，运行中，部分 GPU 占用
- **Draining**: 橙色，排空中，不再接受新任务
- **Unhealthy**: 红色，异常，不可用

**GPU 利用率可视化**：
- 进度条显示（0-100%）
- 颜色编码：<50% 绿色，50-80% 蓝色，>80% 橙色，>95% 红色

#### 3.4.2 Worker 详情页

**布局**：顶部信息卡 + GPU 详细列表

```
┌──────────────────────────────────────────────────────────┐
│  ← Worker 详情: worker-01                     [编辑] [删除]│
├──────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐ │
│  │  基本信息                                          │ │
│  │  • 节点名称: worker-01                             │ │
│  │  • IP 地址: 192.168.1.101                          │ │
│  │  • 状态: Ready ●                                   │ │
│  │  • 注册时间: 2025-01-15 10:30:00                   │ │
│  │  • 最后心跳: 5 秒前                                │ │
│  │  • 标签: gpu-type=a100, zone=prod                 │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  GPU 资源 (4 张 NVIDIA A100 80GB)                  │ │
│  ├────────────────────────────────────────────────────┤ │
│  │  gpu:0 │ ████████░░ 82% │ 65.6GB │ 75°C │ Running │ │
│  │  gpu:1 │ ████████░░ 78% │ 62.4GB │ 71°C │ Running │ │
│  │  gpu:2 │ ████░░░░░░░ 42% │ 33.6GB │ 56°C │ Idle    │ │
│  │  gpu:3 │ ████░░░░░░░ 45% │ 36.0GB │ 58°C │ Idle    │ │
│  └────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────┐ │
│  │  运行中的模型                                      │ │
│  │  • llama-3-8b (2 副本) - gpu:0, gpu:1              │ │
│  │  • qwen-14b (1 副本) - gpu:2                       │ │
│  └────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

**实时更新**：
- GPU 利用率、温度、显存每 5 秒更新
- 使用 WebSocket 推送或轮询
- 变化时高亮显示

#### 3.4.3 添加 Worker 对话框

**布局**：两列布局，左侧输入，右侧说明

```
┌──────────────────────────────────────────────────────────┐
│                    添加 Worker 节点              [×]     │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────┐  ┌────────────────────────────┐ │
│  │  配置信息          │  │  说明                      │ │
│  │                    │  │                            │ │
│  │  节点名称：        │  │  Worker 注册流程：          │ │
│  │  [worker-05    ]   │  │  1. 在 Worker 节点安装 agent │ │
│  │                    │  │  2. 复制注册 Token         │ │
│  │  注册 Token：      │  │  3. 启动 Worker 服务        │ │
│  │  [tm_worker_xyz...] │  │  4. 等待节点上线          │ │
│  │  [复制 Token]      │  │                            │ │
│  │                    │  │  安装命令：                │ │
│  │  标签（可选）：    │  │  curl -sfL https://...      │ │
│  │  [添加标签+]       │  │  \| bash -s -- tm_worker... │ │
│  │  gpu-type: [a100▼] │  │                            │ │
│  │  zone: [prod▼]     │  │  预计上线时间：< 1 分钟     │ │
│  │                    │  │                            │ │
│  └────────────────────┘  └────────────────────────────┘ │
│                                                          │
│                            [取消]           [确认添加]  │
└──────────────────────────────────────────────────────────┘
```

**流程说明**：
1. 用户填写节点名称，系统生成注册 Token
2. 复制 Token 和安装命令
3. 在目标节点执行安装命令
4. 页面显示等待状态，检测到节点上线后自动关闭对话框

---

### 3.5 网关管理

**功能概述**：网关管理模块提供统一的 API 网关配置能力，是实现语义路由、API 聚合、权重分配、动态调度和故障转移的核心控制面板。通过可视化的配置界面，管理员可以灵活配置流量分发策略、监控网关性能、管理 API 密钥权限。

**核心能力**：
- 🔀 **智能路由**：语义路由、权重路由、轮询、最少连接等多种路由策略
- 📊 **API 聚合**：多个模型实例聚合为单一 API 端点，对外统一接口
- ⚖️ **负载均衡**：权重分配、动态调度、流量控制
- 🔄 **故障转移**：健康检查、自动切换、重试机制
- 🔐 **密钥管理**：API Key 创建、权限配置、配额管理
- 📈 **监控统计**：实时监控、使用统计、性能分析

**布局**：标签页设计，6 个子功能模块

```
┌──────────────────────────────────────────────────────────┐
│  网关管理                                                  │
├──────────────────────────────────────────────────────────┤
│  [API密钥管理] [路由策略] [负载均衡] [故障转移] [监控]  │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  [当前选中标签页的内容区域]                                │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

---

#### 3.5.1 API 密钥管理

**功能说明**：管理 API 密钥的完整生命周期，包括创建、权限配置、路由绑定、使用监控等。每个密钥可以绑定专属的路由策略和权限配置。

```
┌──────────────────────────────────────────────────────────────┐
│  API 密钥管理                                     [+ 创建新密钥] │
├──────────────────────────────────────────────────────────────┤
│  筛选: [全部 ▼] [活跃] [已禁用] [已过期]  搜索: [__________] │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Production API Key                        [活跃 ●] [▼]│ │
│  │ tm_sk_ab12...  •  创建: 2025-01-15  •  最后使用: 2分钟前│ │
│  │ ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │ │
│  │ Token: 12.5M / 100M (12.5%)  •  请求: 125K 今日         │ │
│  │                                                        │ │
│  │ 🔗 绑定路由: qwen-智能路由 (90%) + llama-灰度 (10%)  │ │
│  │ 🔐 权限: qwen/llama/glm  •  并发: 10  •  QPS: 100     │ │
│  │                                                        │ │
│  │ [详情] [统计] [编辑] [复制密钥] [禁用]                │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**卡片展示信息**：

| 信息类型 | 说明 |
|---------|------|
| **基本信息** | 密钥名称、前缀、创建时间、最后使用时间、状态 |
| **使用情况** | Token 消耗、请求数、配额进度条 |
| **绑定路由** 🔗 | 关联的路由策略名称和模式 |
| **权限配置** 🔐 | 可访问模型、允许操作、并发/QPS 限制 |

**下拉菜单操作**：
- **详情** - 查看密钥完整信息和配置
- **使用统计** - 跳转到使用统计页面
- **编辑** - 修改密钥配置和权限
- **复制密钥** - 复制完整的 API 密钥
- **禁用/启用** - 切换密钥状态
- **撤销** - 永久删除密钥（需确认）

---

#### 3.5.2 路由策略管理

**功能说明**：配置和管理智能路由策略，支持语义路由、权重路由、轮询、最少连接等多种模式。路由策略可以独立创建，然后绑定到 API 密钥使用。

```
┌──────────────────────────────────────────────────────────────┐
│  路由策略管理                                   [+ 创建路由策略] │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐ │
│  │ qwen-智能路由                    [启用 ●]  [编辑] [删除]│ │
│  │ 模式: 语义路由  •  实例数: 2  •  创建: 2025-01-15    │ │
│  │                                                        │ │
│  │ 路由规则:                                              │ │
│  │ • qwen-2.5-7b (主实例)  ████████████ 90%  •  P1   │ │
│  │ • qwen-2.5-7b (canary) ██          10%  •  P2   │ │
│  │                                                        │ │
│  │ 绑定密钥: 3 个  •  今日请求: 125K  •  P95延迟: 450ms │ │
│  │ [查看详情] [查看监控] [复制配置] [禁用]             │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ llama-灰度发布                  [启用 ●]  [编辑] [删除]│ │
│  │ 模式: 权权路由  •  实例数: 2  •  创建: 2025-01-14    │ │
│  │                                                        │ │
│  │ 路由规则:                                              │ │
│  │ • llama-3-8b-v2 (新版) ████████████ 80%  •  P1   │ │
│  │ • llama-3-8b-v1 (旧版) ██          20%  •  P2   │ │
│  │                                                        │ │
│  │ 绑定密钥: 1 个  •  今日请求: 45K   •  P95延迟: 320ms │ │
│  │ [查看详情] [查看监控] [复制配置] [禁用]             │ │
│  ├────────────────────────────────────────────────────────┤ │
│  │ 统一入口 (Round-Robin)              [启用 ●]  [编辑] [删除]│ │
│  │ 模式: 轮询路由  •  实例数: 4  •  创建: 2025-01-13    │ │
│  │                                                        │ │
│  │ 路由规则:                                              │ │
│  │ • llama-3-8b-1      ████         25%  •  均衡   │ │
│  │ • llama-3-8b-2      ████         25%  •  均衡   │ │
│  │ • qwen-2.5-7b       ████         25%  •  均衡   │ │
│  │ • glm-4-9b          ████         25%  •  均衡   │ │
│  │                                                        │ │
│  │ 绑定密钥: 5 个  •  今日请求: 89K   •  P95延迟: 380ms │ │
│  │ [查看详情] [查看监控] [复制配置] [禁用]             │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

**路由策略类型**：

| 路由模式 | 图标 | 说明 | 适用场景 |
|---------|------|------|---------|
| **语义路由** | 🧠 | 根据模型名称智能匹配 | A/B 测试、模型别名 |
| **权重路由** | ⚖️ | 按指定比例分配流量 | 金丝雀发布、灰度发布 |
| **轮询路由** | 🔄 | 平均分配到所有实例 | 负载均衡 |
| **最少连接** | 📉 | 路由到负载最低的实例 | 性能优化 |

**创建/编辑路由策略**：

```
┌──────────────────────────────────────────────────────────────┐
│                    创建路由策略                  [×]      │
├──────────────────────────────────────────────────────────────┤
│                                                          │
│  基本信息                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 策略名称: [qwen-智能路由________________________]     │ │
│  │ 策略描述: [根据模型名自动匹配到对应实例__________]     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  路由模式                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ⦿ 语义路由    ○ 权重路由    ○ 轮询    ○ 最少连接    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  路由规则配置                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 模型匹配模式 → 目标实例        权重    优先级       │ │
│  │ [qwen.*    ] → [qwen-2.5-7b  ▼] [90%━━] [1▼]       │ │
│  │ [qwen.*    ] → [qwen-2.5-7b-2▼] [10%━━] [2▼]       │ │
│  │ [+ 添加规则]                                          │ │
│  │                                                        │ │
│  │ 💡 提示: 使用正则表达式匹配模型名，优先级越小越高   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  API 端点聚合                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ☑ 启用 API 聚合                                       │ │
│  │ 统一端点路径: [/v1/models/qwen-unified___________]    │ │
│  │                                                        │ │
│  │ 统一端点响应模式:                                     │ │
│  │ ⦿ 返回最佳结果  ○ 返回所有结果  ○ 自定义         │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  绑定 API 密钥                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ☑ Production API Key                                 │ │
│  │ ☑ Development Key                                     │ │
│  │ ☐ Testing Key                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│                          [取消]           [保存策略]   │
└──────────────────────────────────────────────────────────────┘
```

---

#### 3.5.3 负载均衡配置

**功能说明**：配置动态负载均衡策略，基于后端实例的实时负载（队列深度、响应时间、资源利用率）自动分配请求，优化整体性能和资源利用率。

```
┌──────────────────────────────────────────────────────────────┐
│  负载均衡配置                            [全局设置] [刷新]    │
├──────────────────────────────────────────────────────────────┤
│                                                          │
│  全局负载均衡策略                                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 启用动态负载均衡:              ☑ 启用                 │ │
│  │                                                        │ │
│  │ 调度策略:                                              │ │
│  │ ⦿ 队列深度优先  ○ 响应时间优先  ○ 资源利用率优先 │ │
│  │                                                        │ │
│  │ 触发阈值:                                              │ │
│  │ • 队列深度 > [50] 请求时自动切换到空闲实例           │ │
│  │ • 响应时间 > [5000] ms 时切换到更快实例                │ │
│  │ • GPU 利用率 > [95]% 时避免分配新请求                │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  实时负载监控                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 实例名称              队列深度  响应时间  GPU利用率  状态│ │
│  │ qwen-2.5-7b-1        23       250ms     78%        ●   │ │
│  │ qwen-2.5-7b-2        15       280ms     82%        ●   │ │
│  │ qwen-2.5-7b-3        5        310ms     65%        ●   │ │
│  │ llama-3-8b-1        45       420ms     71%        ●   │ │
│  │ llama-3-8b-2        38       390ms     68%        ●   │ │
│  │ glm-4-9b-1          12       380ms     55%        ●   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  [手动重新平衡]  [导出配置]  [导入配置]                  │
└──────────────────────────────────────────────────────────────┘
```

**调度策略详解**：

| 策略 | 说明 | 优点 | 缺点 |
|------|------|------|------|
| **队列深度优先** 📊 | 优先路由到队列最短的实例 | 最大化吞吐量 | 可能忽略响应时间 |
| **响应时间优先** ⚡ | 优先路由到响应最快的实例 | 优化用户体验 | 可能导致负载不均 |
| **资源利用率优先** 💾 | 优先路由到 GPU 利用率低的实例 | 最大化资源利用 | 可能影响性能 |
| **综合评分** 🎯 | 综合考虑多个指标的加权评分 | 平衡多种因素 | 配置复杂 |

---

#### 3.5.4 健康检查与故障转移

**功能说明**：配置后端实例的健康检查机制，定义故障检测规则和自动转移策略，确保服务高可用性。

```
┌──────────────────────────────────────────────────────────────┐
│  健康检查与故障转移                      [健康状态概览]    │
├──────────────────────────────────────────────────────────────┤
│                                                          │
│  全局健康检查配置                                        │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 启用自动故障转移:              ☑ 启用                 │ │
│  │                                                        │ │
│  │ 健康检查配置:                                          │ │
│  │ 检查方式: ⦿ 主动探测  ○ 被动接收                  │ │
│  │ 检查间隔: [10] 秒  •  超时时间: [5] 秒             │ │
│  │                                                        │ │
│  │ 故障判定条件:                                        │ │
│  │ ☑ 连续失败 [3] 次标记为故障                        │ │
│  │ ☑ 响应时间 > [5000] ms 标记为故障                   │ │
│  │ ☑ 错误率 > [10]% 标记为故障                         │ │
│  │ ☑ 队列深度 > [100] 标记为过载                       │ │
│  │                                                        │ │
│  │ 恢复策略:                                              │ │
│  │ ☑ 连续成功 [3] 次自动恢复                          │ │
│  │ ☑ 健康检查通过自动恢复                             │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  实例健康状态                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 实例名称              状态    最后检查  失败次数  操作│ │
│  │ qwen-2.5-7b-1        ●健康  10秒前    0       [详情]│ │
│  │ qwen-2.5-7b-2        ●健康  8秒前     0       [详情]│ │
│  │ qwen-2.5-7b-3        ⚠️警告  5秒前     1       [详情]│ │
│  │ llama-3-8b-1        ●健康  9秒前     0       [详情]│ │
│  │ llama-3-8b-2        🔴故障  2分钟前   5       [详情]│ │
│  │ glm-4-9b-1          ●健康  12秒前    0       [详情]│ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  故障转移历史                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 时间            实例              事件              │ │
│  │ 14:25:30       llama-3-8b-2      标记为故障          │ │
│  │                 → qwen-2.5-7b-3     自动转移            │ │
│  │ 14:23:15       qwen-2.5-7b-1      响应超时          │ │
│  │                 → qwen-2.5-7b-2     自动转移            │ │
│  │ [查看完整历史]                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  [手动触发故障转移]  [查看故障报告]  [导出配置]         │
└──────────────────────────────────────────────────────────────┘
```

**故障转移流程**：

```
┌──────────────────────────────────────────────────────────┐
│  故障转移流程                                          │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  1. 检测阶段                                              │
│  │  ┌──────────┐                                           │ │
│  │  │健康检查  │ →  每隔 10 秒探测所有实例            │ │
│  │  └────┬─────┘                                           │ │
│  │       │                                                 │ │
│  │       ▼                                                 │ │
│  │  ┌──────────┐                                           │ │
│  │  │判定状态  │ →  健康 / 警告 / 故障             │ │
│  │  └────┬─────┘                                           │ │
│  │       │                                                 │ │
│  │  实例故障 ────────────────┐                           │ │
│  │  │                       │                           │ │
│  │  ▼                       ▼                           │ │
│  │  ┌──────────┐         ┌──────────┐                │ │
│  │  │ 标记故障  │         │ 记录日志  │                │ │
│  │  └────┬─────┘         └──────────┘                │ │
│  │       │                                                 │ │
│  │  2. 转移阶段                                              │ │
│  │       │                                                 │ │
│  │       ▼                                                 │ │
│  │  ┌────────────────────────────┐                      │ │
│  │  │ 查找备用健康实例           │                      │ │
│  │  │ • 按优先级排序              │                      │ │
│  │  │ • 检查负载情况              │                      │ │
│  │  │ • 选择最优实例              │                      │ │
│  │  └────────┬───────────────────┘                      │ │
│  │           │                                               │ │
│  │           ▼                                               │ │
│  │  ┌────────────────────────────┐                      │ │
│  │  │ 切换流量                   │                      │ │
│  │  │ • 更新路由表               │                      │ │
│  │  │ • 通知网关                 │                      │ │
│  │  │ • 记录转移日志             │                      │ │
│  │  └────────────────────────────┘                      │ │
│  │                                                        │ │
│  │  3. 恢复阶段                                              │ │
│  │       │                                                 │ │
│  │       ▼                                                 │ │
│  │  ┌────────────────────────────┐                      │ │
│  │  │ 持续健康检查               │                      │ │
│  │  │ • 连续成功 3 次             │                      │ │
│  │  │ • 标记为恢复               │                      │ │
│  │  │ • 重新加入负载池           │                      │ │
│  │  └────────────────────────────┘                      │ │
│  │                                                        │ │
│  └──────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

---

#### 3.5.5 使用统计与监控

**功能说明**：实时监控网关性能指标，包括请求量、响应时间、错误率、Token 消耗等，支持按 API 密钥、路由策略、模型实例等多维度分析。

```
┌──────────────────────────────────────────────────────────────┐
│  网关监控仪表盘                            [实时刷新: 5s]   │
├──────────────────────────────────────────────────────────────┤
│  时间范围: [最近1小时 ▼]  [今天]  [本周]  [自定义]         │
│                                                          │
│  核心指标                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 125.4K   │  │  45ms    │  │  99.8%   │  │  623     │  │
│  │ QPS      │  │ P95延迟  │  │  可用率   │  │  实例数  │  │
│  │ +12%     │  │ -8ms    │  │          │  │  / 8     │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  QPS 趋势 (实时)                                     │ │
│  │  [折线图 - 显示每秒 QPS，支持缩放和拖拽]             │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  响应时间分布 (P50/P95/P99)                           │ │
│  │  [直方图 - 显示响应时间分布]                         │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  按维度分析                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 筛选: [按API密钥 ▼]  [按路由策略 ▼]  [按模型 ▼]       │ │
│  │                                                  │ │
│  │  Top 5 API 密钥 (按请求数)                         │ │
│  │  Production Key ████████████████████ 85K           │ │
│  │  Development Key ███ 12K                          │ │
│  │  Testing Key      █ 3K                            │ │ │
│  │                                                  │ │
│  │  Top 5 路由策略 (按请求数)                         │ │
│  │  qwen-智能路由   ████████████████████ 98K           │ │
│  │  llama-灰度      ███ 15K                          │ │
│  │  统一入口        ██ 12K                          │ │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  [查看详细报告]  [导出数据]  [配置告警]                    │
└──────────────────────────────────────────────────────────────┘
```

**告警配置**：

```
┌──────────────────────────────────────────────────────────────┐
│  配置告警规则                                                  │
├──────────────────────────────────────────────────────────────┤
│                                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 告警规则名称: [QPS 异常告警__________________]        │ │
│  │                                                        │ │
│  │ 监控指标: [QPS ▼]                                    │ │
│  │ 触发条件: [▼] 高于阈值                              │ │
│  │ 阈值: [15000]  持续时间: [5] 分钟                     │ │
│  │                                                        │ │
│  │ 通知方式:                                              │ │
│  │ ☑ 邮件  ☑ Webhook  ☑ 企业微信  ☑ 钉钉             │ │
│  │                                                        │ │
│  │ 通知接收人: [admin@tokenmachine.com________________]  │ │
│  │                                                        │ │
│  │ 已触发次数: 3 次  •  最后触发: 5分钟前               │ │
│  │                                                        │ │
│  │ [保存规则] [测试告警] [删除]                           │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  [+ 添加告警规则]                                          │
└──────────────────────────────────────────────────────────────┘
```

---

#### 3.5.6 创建 API 密钥对话框（集成路由配置）

```
┌──────────────────────────────────────────────────────────────┐
│                    创建 API 密钥                  [×]      │
├──────────────────────────────────────────────────────────────┤
│                                                          │
│  基本信息                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 密钥名称: [Production API Key____________________]   │ │
│  │ 描述说明: [用于生产环境的 API 密钥______________]   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  🔐 权限配置                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 可访问的模型:                                        │ │
│  │ ☑ qwen2.5-7b-prod      ☑ llama-3-8b-staging        │ │
│  │ ☐ glm-4-9b            ☐ mistral-7b                  │ │
│  │                                                        │ │
│  │ 允许的操作:                                            │ │
│  │ ☑ Chat 对话       ☑ Text Completion   ☑ Embedding  │ │
│  │ ☐ Image Generate  ☐ Audio TTS        ☐ Fine-tune   │ │
│  │                                                        │ │
│  │ 并发限制: [10]   QPS 限制: [100]   IP 白名单: [可选] │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  📊 配额管理                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ⦿ 无限制 (生产环境推荐)                               │ │
│  │ ○ 自定义配额                                           │ │
│  │   Token 配额:     [10000000] tokens                   │ │
│  │   请求数限制:     [10000] 次/天                       │ │
│  │   并发数限制:     [10]                                │ │
│  │   费用预算:       [¥1000] /月                         │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  🔀 路由配置（绑定现有路由策略）                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ 选择路由策略:                                          │ │
│  │ [qwen-智能路由 ▼]  [+ 创建新路由]                     │ │
│  │                                                        │ │
│  │ 或为该密钥创建专属路由:                               │ │
│  │ ☑ 创建专属路由  (不共享其他密钥)                     │ │
│  │                                                        │ │
│  │ 专属路由配置:                                          │ │
│  │ • 模式: [语义路由 ▼]                                  │ │
│  │ • 语义规则: [qwen.* → qwen-2.5-7b]                   │ │
│  │ • 权重分配: [90%━━━]  动态调度: ☑ 启用              │ │
│  │ • 故障转移: ☑ 启用  重试次数: [3]                   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  ⏰ 过期时间                                                │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ ⦿ 永不过期                                             │ │
│  │ ○ 30 天后过期                                          │ │
│  │ ○ 自定义日期: [2025-12-31]                           │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                          │
│  说明: 创建成功后，密钥将仅显示一次，请妥善保存。           │
│                                                          │
│                          [取消]               [创建密钥]   │
└──────────────────────────────────────────────────────────────┘
```

---

#### 3.5.7 功能模块总结

| 模块 | 功能 | 对应后端功能 |
|------|------|-------------|
| **API 密钥管理** | 密钥 CRUD、权限配置、配额管理 | 认证鉴权 |
| **路由策略管理** | 语义/权重/轮询/最少连接路由 | 语义路由、权重分配 |
| **负载均衡配置** | 动态调度、队列/响应时间/资源优先级 | 动态调度 |
| **健康检查** | 故障检测、自动转移、恢复策略 | 故障转移 |
| **监控统计** | 实时监控、使用统计、告警配置 | 使用统计 |




## 4. 交互设计

### 4.1 通用交互模式

#### 4.1.1 按钮

| 按钮类型 | 样式 | 使用场景 |
|---------|------|----------|
| **主要按钮** | 蓝色背景，白色文字 | 主要操作（创建、部署、保存） |
| **次要按钮** | 白色背景，蓝色边框 | 辅助操作（取消、返回） |
| **文字按钮** | 无背景，蓝色文字 | 低优先级操作（编辑、查看） |
| **危险按钮** | 红色背景，白色文字 | 破坏性操作（删除、下线） |
| **图标按钮** | 仅图标，悬停提示 | 紧凑空间（刷新、设置） |

**状态**：
- 默认：可点击
- 悬停：背景加深/高亮
- 禁用：灰色，不可点击
- 加载：显示 Loading 动画，禁用点击

#### 4.1.2 表单

**布局原则**：
- 标签在输入框上方（左对齐）
- 必填项标记红色星号
- 输入框高度 36px，宽度根据内容自适应
- 错误提示显示在输入框下方

**验证规则**：
- 实时验证：失焦时验证
- 提交验证：点击提交时验证所有字段
- 错误提示：具体的错误信息，避免模糊提示

**示例**：
```
模型名称 *
[meta-llama/Llama-3-8B-Instruct        ]
✓ 模型格式正确

版本号 *
[v1.0                                        ]
✗ 版本号格式应为 vx.x (如 v1.0, v2.1)
```

#### 4.1.3 反馈提示

**成功提示**：
- 位置：页面右上角
- 样式：绿色背景 + 图标
- 自动关闭：3 秒后消失
- 示例："✓ 部署创建成功"

**错误提示**：
- 位置：页面右上角
- 样式：红色背景 + 图标
- 可手动关闭
- 示例："✗ 部署失败：GPU 资源不足"

**警告提示**：
- 位置：页面右上角
- 样式：橙色背景 + 图标
- 可手动关闭
- 示例："⚠ GPU 温度过高（85°C）"

**加载状态**：
- 按钮：显示 Loading 动画，禁用点击
- 页面：居中显示 Loading 组件
- 骨架屏：列表加载时显示骨架占位

#### 4.1.4 确认对话框

**删除确认**：
```
┌─────────────────────────────────────┐
│  ⚠ 确认删除              [×]       │
├─────────────────────────────────────┤
│                                     │
│  确定要删除模型 "llama-3-8b" 吗？    │
│                                     │
│  此操作将：                          │
│  • 删除模型文件（16.5GB）            │
│  • 停止所有相关部署                  │
│  • 无法恢复                          │
│                                     │
│                [取消]  [确认删除]   │
└─────────────────────────────────────┘
```

**下线 Worker 确认**：
```
┌─────────────────────────────────────┐
│  ⚠ 确认下线 Worker      [×]       │
├─────────────────────────────────────┤
│                                     │
│  确定要下线 Worker "worker-01" 吗？  │
│                                     │
│  此操作将：                          │
│  • Worker 停止接受新任务             │
│  • 等待当前任务完成                  │
│  • 迁移已有模型实例                  │
│                                     │
│  预计耗时：约 5 分钟                 │
│                                     │
│                [取消]  [确认下线]   │
└─────────────────────────────────────┘
```

### 4.2 实时数据更新

**更新策略**：

| 数据类型 | 更新方式 | 频率 | 优化 |
|---------|---------|------|------|
| **GPU 监控** | 轮询 | 5 秒 | 仅更新可见区域 |
| **部署状态** | WebSocket + 轮询补偿 | 实时 | 状态变化时推送 |
| **模型列表** | 手动刷新 | - | 下拉刷新 |
| **统计数据** | 轮询 | 1 分钟 | 缓存聚合数据 |

**乐观更新**：
- 用户操作立即更新 UI
- 后台异步请求确认
- 失败时回滚并提示

**增量更新**：
- 大列表使用虚拟滚动
- 仅更新变化的数据项
- 使用 diff 算法最小化 DOM 操作

---

## 5. 数据流与状态管理

### 5.1 状态管理架构

采用 **Zustand** 进行状态管理，分层设计：

```
全局状态 (Store)
├── userStore (用户状态)
│   ├── userInfo
│   ├── permissions
│   └── isAuthenticated
│
├── modelStore (模型状态)
│   ├── models: Model[]
│   ├── selectedModel: Model
│   ├── filters: FilterState
│   └── loading: boolean
│
├── deploymentStore (部署状态)
│   ├── deployments: Deployment[]
│   ├── selectedDeployment: Deployment
│   ├── instances: Instance[]
│   └── statusMap: Map<string, Status>
│
├── clusterStore (集群状态)
│   ├── workers: Worker[]
│   ├── gpus: GPU[]
│   ├── metrics: Metrics
│   └── loading: boolean
│
├── apiStore (API 密钥状态)
│   ├── apiKeys: ApiKey[]
│   ├── usageStats: UsageStats
│   └── loading: boolean
│
└── uiStore (UI 状态)
    ├── sidebarCollapsed: boolean
    ├── notifications: Notification[]
    ├── theme: 'light' | 'dark'
    └── currentRoute: string
```

### 5.2 数据流向

**单向数据流**：
```
用户操作 → Action → API 请求 → 更新 Store → 重新渲染 → UI 更新
```

**示例：创建部署**
```
1. 用户点击"创建部署"
2. 调用 deploymentStore.createDeployment(data)
3. 发送 POST /api/v1/deployments
4. 乐观更新：deploymentStore.deployments.push(newDeployment)
5. 等待响应
6. 成功：更新 deploymentStore 状态
7. 失败：回滚乐观更新，显示错误提示
```

### 5.3 API 集成

**API 客户端封装**：
- 统一的错误处理
- 自动重试机制
- 请求/响应拦截器
- Token 自动刷新

**API 分类**：

| 类别 | 前缀 | 说明 |
|------|------|------|
| **认证** | `/api/v1/auth` | 登录、登出、Token 刷新 |
| **模型管理** | `/api/v1/models` | 模型 CRUD、下载 |
| **部署管理** | `/api/v1/deployments` | 部署 CRUD、启停 |
| **集群管理** | `/api/v1/cluster` | Worker 管理 |
| **API 密钥** | `/api/v1/api-keys` | 密钥 CRUD |
| **OpenAI API** | `/v1` | Chat Completions、Models |
| **监控** | `/api/v1/metrics` | Prometheus 指标查询 |

---

## 6. 组件设计

### 6.1 基础组件

#### 6.1.1 状态标签 (StatusBadge)

**用途**：显示实体状态（模型、部署、Worker）

**样式**：
- Ready: 绿色背景 (#52c41a)
- Running: 蓝色背景 (#1890ff)
- Starting: 橙色背景 (#fa8c16)
- Error: 红色背景 (#f5222d)
- Disabled: 灰色背景 (#d9d9d9)

**尺寸**：
- Small: 高度 20px，字号 12px
- Default: 高度 24px，字号 14px
- Large: 高度 28px，字号 16px

#### 6.1.2 进度条 (ProgressBar)

**用途**：显示任务进度（模型下载、部署启动）

**类型**：
- 线性进度条：默认
- 环形进度条：用于卡片、图标中

**状态**：
- 正常：蓝色
- 成功：绿色
- 警告：橙色
- 错误：红色

**功能**：
- 显示百分比
- 显示当前/总量（如 45% | 4.5GB / 10GB）
- 支持动画效果

#### 6.1.3 GPU 卡片 (GPUCard)

**用途**：显示单个 GPU 的资源使用情况

**布局**：
```
┌──────────────────────────────┐
│ GPU 0: NVIDIA A100           │
│ ████████░░ 82%               │
│                              │
│ 显存: 65.6GB / 80GB          │
│ 温度: 75°C                   │
│ 状态: Running ●              │
│                              │
│ 部署: llama-3-8b (2 副本)    │
└──────────────────────────────┘
```

**交互**：
- 点击查看详情
- 悬停显示更多信息
- 拖拽调整 GPU 顺序

#### 6.1.4 密码输入框 (PasswordInput)

**用途**：敏感信息输入（密码、Token）

**功能**：
- 显示/隐藏切换
- 密码强度指示
- 自动生成密码
- 一键复制

### 6.2 业务组件

#### 6.2.1 模型列表 (ModelList)

**功能**：
- 列表展示
- 搜索过滤
- 排序分页
- 批量操作
- 拖拽排序

#### 6.2.2 部署配置表单 (DeploymentForm)

**功能**：
- 分步配置
- 实时验证
- 智能推荐
- 成本预估
- 配置导入/导出

#### 6.2.3 实时监控图表 (MetricChart)

**图表类型**：
- 折线图：趋势数据
- 柱状图：统计数据
- 饼图：占比数据
- 仪表盘：单指标

**功能**：
- 实时更新
- 时间范围选择
- 数据缩放
- 图例筛选
- 数据导出

#### 6.2.4 对话组件 (ChatInterface)

**功能**：
- 多轮对话
- 流式输出
- Markdown 渲染
- 代码高亮
- 一键复制
- 导出对话记录

---

## 7. 视觉设计规范

### 7.1 色彩系统

#### 7.1.1 主色调

| 颜色 | 用途 | Hex Code |
|------|------|----------|
| **主色 (Primary)** | 主要按钮、链接、高亮 | #1890ff |
| **成功色 (Success)** | 成功状态、正确操作 | #52c41a |
| **警告色 (Warning)** | 警告提示、待处理 | #faad14 |
| **错误色 (Error)** | 错误状态、危险操作 | #f5222d |
| **信息色 (Info)** | 信息提示、中性状态 | #1890ff |

#### 7.1.2 中性色

| 颜色 | 用途 | Hex Code |
|------|------|----------|
| **标题黑** | 主标题 | #262626 |
| **正文黑** | 正文内容 | #595959 |
| **次要灰** | 次要文本 | #8c8c8c |
| **禁用灰** | 禁用状态 | #bfbfbf |
| **边框灰** | 边框、分割线 | #d9d9d9 |
| **背景灰** | 背景色 | #f0f2f5 |

#### 7.1.3 功能色

| 场景 | 颜色 | Hex Code |
|------|------|----------|
| **GPU 空闲** | 绿色 | #52c41a |
| **GPU 中等负载** | 蓝色 | #1890ff |
| **GPU 高负载** | 橙色 | #fa8c16 |
| **GPU 过载** | 红色 | #f5222d |
| **Worker Ready** | 绿色 | #52c41a |
| **Worker Busy** | 蓝色 | #1890ff |
| **Worker Unhealthy** | 红色 | #f5222d |

### 7.2 字体系统

| 字体 | 用途 | 大小 | 字重 |
|------|------|------|------|
| **主标题** | 页面标题 | 24px | 600 (Semibold) |
| **次标题** | 区块标题 | 18px | 600 (Semibold) |
| **三级标题** | 卡片标题 | 16px | 500 (Medium) |
| **正文** | 内容文本 | 14px | 400 (Regular) |
| **辅助文字** | 说明文字 | 12px | 400 (Regular) |
| **代码** | 代码显示 | 13px | 400 (Mono) |

**字体栈**：
```
系统字体: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif
代码字体: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', monospace
```

### 7.3 间距系统

使用 4px 基础单位：

| 名称 | 值 | 用途 |
|------|------|------|
| **xs** | 4px | 紧密元素间距 |
| **sm** | 8px | 小元素间距 |
| **md** | 16px | 默认元素间距 |
| **lg** | 24px | 大元素间距 |
| **xl** | 32px | 页面区块间距 |
| **xxl** | 48px | 大区块间距 |

### 7.4 圆角系统

| 名称 | 值 | 用途 |
|------|------|------|
| **sm** | 2px | 标签、按钮 |
| **md** | 4px | 输入框、卡片 |
| **lg** | 8px | 对话框、面板 |

### 7.5 阴影系统

| 名称 | 值 | 用途 |
|------|------|------|
| **sm** | 0 1px 2px rgba(0,0,0,0.03) | 卡片悬停 |
| **md** | 0 2px 8px rgba(0,0,0,0.08) | 下拉菜单 |
| **lg** | 0 4px 16px rgba(0,0,0,0.12) | 对话框 |
| **xl** | 0 8px 24px rgba(0,0,0,0.16) | 抽屉、模态框 |

---

## 8. 响应式设计

### 8.1 断点系统

| 断点 | 宽度 | 设备 |
|------|------|------|
| **xs** | < 576px | 手机（竖屏） |
| **sm** | 576px - 768px | 手机（横屏） |
| **md** | 768px - 992px | 平板 |
| **lg** | 992px - 1200px | 小屏笔记本 |
| **xl** | 1200px - 1600px | 桌面 |
| **xxl** | > 1600px | 大屏显示器 |

### 8.2 布局适配

**侧边栏**：
- 大屏（≥992px）：固定显示
- 中屏（768-992px）：可折叠
- 小屏（<768px）：抽屉式

**表格**：
- 大屏：显示所有列
- 中屏：隐藏次要列
- 小屏：卡片式布局

**图表**：
- 大屏：完整显示
- 中屏：调整比例
- 小屏：横向滚动

### 8.3 移动端优化

**触摸优化**：
- 按钮最小尺寸：44x44px
- 增大点击热区
- 避免误触

**手势支持**：
- 下拉刷新
- 侧滑返回
- 长按菜单

**性能优化**：
- 图片懒加载
- 列表虚拟化
- 减少动画

---

## 9. 性能优化策略

### 9.1 加载优化

**首屏优化**：
- 代码分割：路由级别分割
- 懒加载：非首屏组件延迟加载
- 预加载：预测用户行为，预加载资源

**资源优化**：
- 图片压缩：WebP 格式
- 字体子集化：仅加载所需字符
- CSS/JS 压缩：Gzip/Brotli

**CDN 加速**：
- 静态资源 CDN
- API 请求就近接入

### 9.2 渲染优化

**虚拟滚动**：
- 大列表使用虚拟滚动
- 仅渲染可见区域

**防抖节流**：
- 搜索输入：300ms 防抖
- 滚动事件：100ms 节流
- 窗口缩放：200ms 防抖

**批量更新**：
- 合并多次状态更新
- 使用 requestAnimationFrame

### 9.3 数据优化

**缓存策略**：
- HTTP 缓存：强缓存 + 协商缓存
- 本地缓存：LocalStorage/SessionStorage
- 内存缓存：Map/Set 数据结构

**请求优化**：
- 请求合并：批量操作
- 请求取消：组件卸载时取消请求
- 请求去重：相同请求复用

**数据预取**：
- 预取下一页数据
- 预取关联数据

### 9.4 监控与分析

**性能指标**：
- FCP (First Contentful Paint): < 1s
- LCP (Largest Contentful Paint): < 2.5s
- TTI (Time to Interactive): < 3s
- CLS (Cumulative Layout Shift): < 0.1

**监控工具**：
- Web Vitals: 核心性能指标
- Lighthouse: 性能评分
- Sentry: 错误监控

---

## 10. 可访问性 (Accessibility)

### 10.1 键盘导航

- 所有交互元素支持键盘访问
- Tab 顺序合理
- 焦点状态清晰可见
- 快捷键支持：
  - `Ctrl/Cmd + K`: 搜索
  - `Ctrl/Cmd + /`: 快捷键帮助
  - `Esc`: 关闭对话框/抽屉

### 10.2 屏幕阅读器

- 语义化 HTML
- ARIA 属性完善
- 图片 alt 文本
- 表单标签关联

### 10.3 色彩对比度

- 文本对比度 ≥ 4.5:1 (WCAG AA)
- 大文本对比度 ≥ 3:1
- 不仅依赖颜色传达信息

---

## 11. GPUStack UI 参考设计

> 本章节基于对 [GPUStack UI](https://github.com/gpustack/gpustack-ui) 的深入分析，提炼可参考的设计模式和架构方案。

### 11.1 技术栈对比

| 方面 | GPUStack UI | TokenMachine | 建议 |
|------|-------------|--------------|------|
| **构建工具** | UmiJS Max | Vite | 保持 Vite，更轻量灵活 |
| **UI 框架** | Ant Design 6 + ProComponents | Ant Design 6 | 考虑引入 ProComponents |
| **状态管理** | Jotai (原子化状态) | Zustand | 两者都适合 |
| **样式方案** | styled-components | 待定 | 可参考 CSS-in-JS 方案 |
| **路由模式** | Hash Router | 待定 | 建议 Hash Router（兼容性） |
| **国际化** | react-intl (Umi 内置) | 待定 | 推荐 react-intl |

### 11.2 核心组件设计参考

#### 11.2.1 SealTable - 增强型表格组件

**特性**：
- 支持嵌套数据展开（父子关系）
- 多列排序（单列/多列切换）
- 虚拟滚动（大数据量）
- 行选择（单选/多选）
- 实时数据更新集成
- 自定义单元格渲染

**代码示例**：
```typescript
<SealTable
  columns={columns}
  dataSource={dataSource}
  rowKey="id"
  rowSelection={{
    selectedRowKeys,
    onChange: handleSelectionChange
  }}
  expandable={{
    childrenDataSource: modelInstances,
    childrenRowKey: "id"
  }}
  pagination={{
    current: page,
    pageSize: perPage,
    total
  }}
  onTableSort={handleSortChange}
/>
```

**适用场景**：
- 模型列表（可展开查看实例）
- Worker 列表（可展开查看 GPU）
- API Key 列表（可展开查看使用记录）

#### 11.2.2 SealForm - 浮动标签表单

**特性**：
- Material Design 风格的浮动标签
- 自动状态管理（focus/blur/error）
- 统一的验证样式
- 支持多种输入类型

**代码示例**：
```typescript
<SealInput
  label="模型名称"
  placeholder="请输入模型名称"
  required
  value={name}
  onChange={(e) => setName(e.target.value)}
  status={error ? 'error' : ''}
  description="仅支持字母、数字和连字符"
/>
```

#### 11.2.3 YAMLEditor - YAML 配置编辑器

**特性**：
- 基于 Monaco Editor
- 语法高亮和自动补全
- YAML Schema 验证
- 导入/导出文件

**代码示例**：
```typescript
<YAMLEditor
  value={yamlContent}
  height={400}
  schema={backendSchema}
  placeholder="配置后端参数..."
  validateMessage={error}
  onUpload={handleImportYaml}
  ref={editorRef}
/>
```

### 11.3 实时数据更新机制

#### 11.3.1 Watch API + Web Worker

**架构**：
```
┌─────────────┐     SSE      ┌─────────────┐     Worker     ┌─────────────┐
│   前端组件   │ ←────────→  │  Hook 层    │ ←──────────→  │  JSON 解析   │
│  (UI 渲染)  │   实时流     │ useChunkReq │   流式解析     │  (后台线程)  │
└─────────────┘             └─────────────┘               └─────────────┘
```

**核心 Hook**：
```typescript
// useChunkRequest.ts
const useChunkRequest = () => {
  const [data, setData] = useState([]);
  const workerRef = useRef<Worker>();

  const setupChunkRequest = async (url: string) => {
    // 1. 创建 Web Worker
    workerRef.current = new Worker('./json-parser-worker.ts');

    // 2. 监听 Worker 消息
    workerRef.current.onmessage = (e) => {
      const validJSON = e.data;
      updateDataList(validJSON);  // 乐观更新
    };

    // 3. 发起 SSE 请求
    await request(url, {
      params: { watch: true },
      onDownloadProgress: (e) => {
        const newData = sliceResponseData(e.response);
        workerRef.current.postMessage(newData);
      }
    });
  };

  return { setupChunkRequest };
};
```

**适用场景**：
- GPU 指标实时监控
- 模型部署状态更新
- Worker 心跳检测
- 日志流式输出

#### 11.3.2 数据更新策略

| 策略 | 说明 | 使用场景 |
|------|------|----------|
| **乐观更新** | 先更新 UI，后确认 | 用户操作反馈 |
| **轮询补偿** | SSE + 定时轮询 | 网络不稳定时 |
| **增量更新** | 仅更新变化项 | 大列表优化 |
| **去重合并** | 相同事件合并 | 防止闪烁 |

### 11.4 路由与权限控制

#### 11.4.1 路由配置

```typescript
// config/routes.ts
export default [
  {
    path: '/models',
    key: 'models',
    access: 'canSeeAdmin',  // 权限标识
    routes: [
      {
        path: '/models/catalog',
        key: 'modelsCatalog',
        component: './llmodels/catalog'
      },
      {
        path: '/models/deployments',
        key: 'modelDeployments',
        component: './llmodels/index'
      }
    ]
  }
];
```

#### 11.4.2 权限控制

```typescript
// 权限检查 Hook
const useAccess = () => {
  const [userInfo] = useAtom(userAtom);

  const canSeeAdmin = useMemo(() => {
    return userInfo?.is_admin === true;
  }, [userInfo]);

  return {
    canSeeAdmin,
    canSeeUser: true  // 普通用户可见
  };
};

// 路由守卫
const onPageChange = (location: Location) => {
  if (!initialState?.currentUser && location.pathname !== '/login') {
    history.push('/login');
  }
};
```

### 11.5 Hooks 封装建议

| Hook | 功能 | 优先级 |
|------|------|--------|
| **useTableFetch** | 表格数据获取 + 分页 + 排序 | 高 |
| **useChunkRequest** | 实时数据流处理 | 高 |
| **useTableSort** | 多列排序状态管理 | 中 |
| **useUpdateChunkedList** | 列表增量更新 | 中 |
| **useWorkerMaintenance** | Worker 维护模式 | 低 |
| **useTerminalTabs** | 终端多标签页 | 低 |

#### 11.5.1 useTableFetch 示例

```typescript
const useTableFetch = <T,>({
  fetchAPI,
  deleteAPI,
  defaultQueryParams,
  watch = false
}: Options) => {
  const [dataSource, setDataSource] = useState<{
    dataList: T[];
    loading: boolean;
    total: number;
  }>({
    dataList: [],
    loading: false,
    total: 0
  });

  const [queryParams, setQueryParams] = useState(defaultQueryParams);

  const fetchData = async (params?: Partial<QueryParams>) => {
    const res = await fetchAPI({ ...queryParams, ...params });
    setDataSource({
      dataList: res.items,
      loading: false,
      total: res.pagination.total
    });
  };

  // 如果启用 watch，建立 SSE 连接
  useEffect(() => {
    if (watch) {
      const { setupChunkRequest } = useChunkRequest();
      setupChunkRequest(`${API_URL}?watch=true`);
    }
  }, []);

  return {
    dataSource,
    queryParams,
    fetchData,
    handlePageChange,
    handleSearch,
    handleDelete
  };
};
```

### 11.6 页面设计参考

#### 11.6.1 Dashboard 改进建议

**参考 GPUStack 设计**：
- 多维度指标卡片（运行模型、GPU 总数、使用率、调用量）
- GPU 监控图表（多线折线图，实时更新）
- 活动日志（最新操作、告警信息）
- 资源使用趋势（时间序列图）

**优化点**：
```typescript
// 数据更新策略
const useDashboardData = () => {
  const [metrics, setMetrics] = useState();

  // 1. 初始加载
  useEffect(() => {
    fetchMetrics();
  }, []);

  // 2. 实时更新（Watch API）
  useEffect(() => {
    const { setupChunkRequest } = useChunkRequest();
    setupChunkRequest('/api/metrics?watch=true');

    return () => {
      // 清理连接
    };
  }, []);

  // 3. 定时轮询补偿（5秒）
  useEffect(() => {
    const timer = setInterval(() => {
      fetchMetrics();
    }, 5000);

    return () => clearInterval(timer);
  }, []);

  return metrics;
};
```

#### 11.6.2 模型管理页面

**布局建议**：
```
┌─────────────────────────────────────────────────────┐
│  模型管理                    [上传模型] [批量操作▼]  │
├─────────────────────────────────────────────────────┤
│  🔍 [搜索]              [状态筛选▼] [类别筛选▼]      │
│  ┌───────────────────────────────────────────────┐ │
│  │ 模型名称    │ 版本 │ 大小  │ 状态 │ 操作      │ │
│  ├───────────────────────────────────────────────┤ │
│  │ llama-3-8b │ v1.0│ 16.5GB│ Ready│ [部署] [▼]│ │
│  │            │      │       │      │           │ │
│  │   └─ 实例1 │      │  GPU 0 │ Running│ [停止]│ │
│  │   └─ 实例2 │      │  GPU 1 │ Running│ [停止]│ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**关键特性**：
- 展开行显示模型实例
- 实时状态更新（Running/Pending/Stopped）
- 批量操作（部署/停止/删除）
- GPU 资源可视化

#### 11.6.3 Worker/集群管理

**参考 GPUStack 的 Worker 页面**：
```typescript
// Worker 列表
<Table
  columns={[
    { title: '节点名称', dataIndex: 'name' },
    { title: '状态', dataIndex: 'state', render: StatusTag },
    { title: 'GPU', dataIndex: 'gpu_count' },
    {
      title: '利用率',
      dataIndex: 'mem_usage',
      render: (value) => <ProgressBar percent={value} />
    },
    { title: '操作', render: Actions }
  ]}
  expandable={{
    expandedRowRender: (record) => <WorkerGpuDetail worker={record} />
  }}
/>

// 终端集成
const { TerminalPanel, handleAddTerminal } = useTerminalTabs();

<XTerminal
  socketUrl={`ws://api/workers/${id}/terminal`}
  onData={handleTerminalData}
/>
```

### 11.7 组件库清单

基于 GPUStack UI 分析，建议实现以下组件：

| 组件 | 用途 | 复杂度 | 优先级 |
|------|------|--------|--------|
| **SealTable** | 数据列表 | 高 | P0 |
| **SealForm** | 表单输入 | 中 | P0 |
| **YAMLEditor** | 配置编辑 | 中 | P1 |
| **LogsViewer** | 日志查看 | 中 | P1 |
| **XTerminal** | SSH 终端 | 高 | P2 |
| **FormDrawer** | 抽屉表单 | 低 | P1 |
| **StatusTag** | 状态标签 | 低 | P0 |
| **ProgressBar** | 进度条 | 低 | P0 |
| **FilterBar** | 筛选工具栏 | 中 | P1 |
| **EmptyState** | 空状态 | 低 | P1 |

### 11.8 关键注意事项

#### 11.8.1 性能优化

```typescript
// 1. 虚拟滚动
import { List } from 'react-virtualized';

<List
  width={800}
  height={600}
  rowCount={dataList.length}
  rowHeight={60}
  rowRenderer={({ index, key, style }) => (
    <div key={key} style={style}>
      {renderRow(dataList[index])}
    </div>
  )}
/>

// 2. 防抖搜索
const debouncedSearch = useDebounceFn(
  (value) => {
    handleSearch(value);
  },
  { wait: 300 }
);

// 3. 请求取消
useEffect(() => {
  const source = axios.CancelToken.source();

  fetchData({ cancelToken: source.token });

  return () => {
    source.cancel('Component unmounted');
  };
}, []);
```

#### 11.8.2 错误处理

```typescript
// 统一错误处理
const useApiCall = () => {
  const [error, setError] = useState();

  const call = async (apiFunc) => {
    try {
      return await apiFunc();
    } catch (err) {
      if (axios.isCancel(err)) {
        console.log('Request canceled');
        return;
      }
      setError(err);
      message.error(err.message);
      throw err;
    }
  };

  return { call, error };
};
```

#### 11.8.3 类型安全

```typescript
// API 响应类型
export interface ListResponse<T> {
  items: T[];
  pagination: {
    total: number;
    totalPage: number;
    page: number;
    perPage: number;
  };
}

// 使用示例
const res = await queryModelsList<ModelsResponse>(params);
```

### 11.9 实施建议

**阶段 1：基础组件（1-2 周）**
- SealTable（表格组件）
- SealForm（表单组件）
- StatusTag（状态标签）
- EmptyState（空状态）

**阶段 2：核心页面（2-3 周）**
- 模型管理页
- Dashboard
- Worker 列表页

**阶段 3：高级功能（2-3 周）**
- 实时数据更新
- YAMLEditor
- 日志查看器

**阶段 4：优化与测试（1-2 周）**
- 性能优化
- E2E 测试
- 文档完善

---

**文档版本**: v4.1
**最后更新**: 2025-01-16
**维护者**: TokenMachine Team
